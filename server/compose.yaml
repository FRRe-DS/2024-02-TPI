# https://docs.docker.com/compose/gettingstarted/
services:
  rest_api_django:
    build: .
    ports:
      - "8000:8000"
    develop:
      watch:
        - action: sync
          path: .
          target: /app
  redis:
    image: "redis@alpine"
